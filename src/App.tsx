/**
 * @license
 * SPDX-License-Identifier: Apache-2.0
 *
 * React shell is currently unused for the main game loop.
 * The live entry point is `src/main.ts` (PixiJS + UIManager HUD).
 * This component is kept for future hybrid React overlays / AI Studio templates.
 */
export default function App() {
  return null;
}
