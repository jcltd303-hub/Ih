/**
 * Browser boss-raid results are presentation state, never a wallet authority.
 * Financial boss rewards must be issued by a trusted server transaction.
 */
export function getClientAwardableBossBounty(_result: unknown): number {
  return 0;
}
